### JavaScript

#### npm

`npm install` - install all dependencies

#### Express.js

`npx express-generator .` - create project structure

#### Vue.js

`npm install -g @vue/cli` - install Vue CLI

`vue create .` - create project structure

`vue add bootstrap-vue` - install Bootstrap

`vue add vuetify` - install Vuetify
