# MS-SQL

create database

``` sql
CREATE DATABASE [database];
```

delete table

``` sql
DROP TABLE [table];
```

insert row

``` sql
INSERT INTO [table] (column, ...)
VALUES (value, ...);
```

edit row

``` sql
UPDATE [table] SET [column] = [value];
```

delete rows

``` sql
DELETE FROM [table];
```



